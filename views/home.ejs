<!DOCTYPE html>
<html lang="en">

<head>
  <title>Home</title>
  <%- include("templates/head") %>

  <style>
    .hidden {
      display: none;
    }

    nav.navbar {
      position: relative;

      #nav-container {
        margin: 0 !important;
      }
    }

    #container {
      justify-content: unset;

      #wrapper {
        margin-top: auto;
        margin-bottom: auto;
        gap: 100px;

        p {
          color: rgb(170, 170, 170);
        }
      }
    }
  </style>
</head>

<body>
  <div id="container">
    <div id="wrapper" class="d-flex flex-row align-items-center justify-content-center">
      <div class="d-flex flex-column justify-content-center">
        <h1 class="fs-1 m-0"><%= anon ? 'Please sign up or login' : 'Hello, ' + name %></h1>

        <p class="fs-4 ps-1 m-0 mt-1 <%= anon ? 'd-none' : '' %>">Welcome back</p>
      </div>

      <%- include("templates/nav") %>
    </div>

    <%- include("templates/footer") %>
  </div>
</body>

</html>