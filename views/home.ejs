<!DOCTYPE html>
<html lang="en">

<head>
  <title>Home</title>
  <%- include("templates/head") %>

  <style>
    .hidden {
      display: none;
    }

    nav.navbar {
      position: relative;

      #nav-container {
        margin: 0 !important;
        gap: 8px !important;

        ul li:last-child {
          padding-left: 5px !important;
        }
      }
    }

    #container {
      justify-content: unset;

      #wrapper {
        margin-top: auto;
        margin-bottom: auto;
        gap: 5vw;

        p {
          color: rgb(170, 170, 170);
        }
      }
    }

    @media (width <=1045px) {
      #container #wrapper {
        flex-direction: column !important;
        gap: 40px;
      }
    }
  </style>
</head>

<body>
  <div id="container">
    <div id="wrapper" class="d-flex flex-row align-items-center justify-content-center mx-5">
      <div class="d-flex flex-column justify-content-center">
        <h1 class="fs-1 m-0"><%= anon ? 'Please sign up or login' : 'Hello, ' + name %></h1>

        <p class="fs-4 ps-1 m-0 mt-1 <%= anon ? 'd-none' : '' %>">Welcome back</p>
      </div>

      <%- include("templates/nav") %>
    </div>

    <%- include("templates/footer") %>
  </div>
</body>

</html>